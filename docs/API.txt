
user{
  user anlegen: POST{
        e-mail,
        psswd(Hash),
        userName,
        dateOfBirth,
        paymentMethodID,
        country,
        aboModelID
  }

  user löschen: DELETE{
        userID,
        psswd(Hash)
  }

  update user: PUT{
        e-mail,
        psswd(Hash),
        psswd-Bestägigung(Hash)
        userName,
        dateOfBirth,
        paymentMethodID,
        country,
        aboModelID
  }

}

userGenre{
    anlegen:POST{
        userID,
        genreID
    }

    löschen:DELETE{
        userID,
        genreID
    }
}

borrowed{
    ausleihen: POST{
        userID,
        bookID
    }
}